<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" start-state="orderToPayShowView"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

	<!-- Zabezpieczenie flow przez Spring Security -->
	<secured attributes="ROLE_USER"/>
	
	<var name="controllerVar" class="systemy.bankowe.flows.money.transfer.OrderToPayFlow"/>
	
	<on-start>
		<evaluate expression="controllerVar.getOrderToPays(springSecurityContextUtilBean.getLoggedInUser().get())"
			result="flowScope.orderToPays" />
	</on-start>
	
	<view-state id="orderToPayShowView">
		<transition on="orderToPayShow" to="orderToPayShow"/>
	</view-state>
<!-- 
	<action-state id="Show">
		<evaluate expression="controllerVar.submit(orderToPayVar)" result="flowScope.status" />
		<transition to="orderToPayEnd" />
	</action-state>

		<view-state id="orderToPayEnd" />
		 -->
</flow>
